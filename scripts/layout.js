var layout_module = new function () {
    /***** PRIVATE VARIABLES *****/

    /***** PRIVATE METHODS *****/

    /***** PUBLIC INTERFACE *****/
    return {
        init: function(container_id, uri, positioning) {
            console.log("Init layout.js");

            //connect(uri);
        }
    }
}